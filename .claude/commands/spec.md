구글 드라이브에서 와썹하우스 명세서를 읽고, "$ARGUMENTS"와 관련된 내용만 찾아서 요약한다.

인자가 없으면 "검색할 키워드를 인자로 전달하세요. 예: /spec 게더링 신청"을 출력하고 종료한다.

---

`Agent` 도구로 `.claude/agent/spec-reader.md`에 정의된 `spec-reader` 에이전트를 호출한다.

프롬프트에 다음 내용을 포함한다:
- 검색 키워드: "$ARGUMENTS"
- "구글 드라이브에서 와썹하우스 명세서(요구사항 명세서, API 명세서, 화면 설계서)를 검색하고, '$ARGUMENTS'와 관련된 내용만 찾아서 요약해줘."

에이전트의 결과를 그대로 출력한다.
